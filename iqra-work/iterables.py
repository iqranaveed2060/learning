#  range object is iterable we can iterate over it or use in for loop
# range fn return a range object which is iterable mean x will have different value
# only range objects are not iterable in pyt but str are also iterable


    # add list use square bracktes fot showing list

for x in [1,2,3,4]:
        print (x)
    
               